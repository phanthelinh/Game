#pragma once
#include <vector>

class Cell
{
private:
	int		x, y;
	//AABB cellBounds;
	//std::vector<Body*> bodies;
	
public:
	Cell(int x, int x, int width, int height);
	//void Add(Body* body);
	//void Remove(Body* body);
};